# Spield frontend — environment configuration (Vite, all keys MUST be VITE_-prefixed).
#
# The frontend is env-driven and supports both Stellar networks from one codebase.
# Pick a network with VITE_NETWORK; every other value has a sensible per-network
# default baked into src/lib/config.ts, so usually you only need VITE_NETWORK.
#
# Vite loads these files automatically (later overrides earlier):
#   .env                 -> always loaded        (we default it to TESTNET)
#   .env.production      -> loaded on `vite build`/`pnpm build` (we set MAINNET)
#   .env.development     -> loaded on `vite dev`
#   .env.local           -> always loaded, git-ignored (your personal overrides)
#
# Quick start:
#   - Local dev / staging  -> nothing needed; `.env` already targets testnet.
#   - Production build      -> `pnpm build` picks up `.env.production` (mainnet).
#   - Force a one-off       -> `VITE_NETWORK=mainnet pnpm dev`  (or use .env.local)

# Which network to target: "testnet" (default) or "mainnet".
VITE_NETWORK=testnet

# ── Optional overrides ──────────────────────────────────────────────────────────
# All of these default from the selected network's profile in src/lib/config.ts.
# Only set them to point at a DIFFERENT deployment than the shipped defaults.

# Endpoints
# VITE_RPC_URL=https://soroban-testnet.stellar.org
# VITE_HORIZON_URL=https://horizon-testnet.stellar.org
# VITE_EXPLORER_URL=https://stellar.expert/explorer/testnet
# VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Contract + asset addresses (override only to use a fresh deployment)
# VITE_WRAPPER=
# VITE_STRATEGY=
# VITE_VAULT=
# VITE_MARKET=
# VITE_PT=
# VITE_YT=
# VITE_USDC=
# VITE_PT_YT_ISSUER=

# Misc
# VITE_BACKEND_URL=https://spield-protocol-waitlistbackend.vercel.app

# ── Cross-chain bridge (Allbridge Core) ─────────────────────────────────────────
# Allbridge Core is MAINNET-ONLY (the SDK has no testnet). On a testnet build the
# bridge UI shows live prices/conversions but disables execution; it unlocks on the
# mainnet build automatically.
#
# Required to sign bridge transfers FROM EVM/Solana chains: a free WalletConnect /
# Reown Cloud project id from https://cloud.reown.com. Without it, only Stellar-
# source bridging works (it uses the app's existing Stellar wallet).
# VITE_REOWN_PROJECT_ID=

# Per-chain RPCs the bridge SDK uses to read balances and build/simulate txs on
# source chains. The SDK has NO bundled EVM RPCs, so public defaults ship in
# src/lib/config.ts for every supported chain — bridging works out of the box.
# Public endpoints can rate-limit, so set your own (Infura/Alchemy/QuickNode…) for
# production. Override any of these:
# VITE_BRIDGE_RPC_SOL=https://api.mainnet-beta.solana.com
# VITE_BRIDGE_RPC_ETH=https://eth.llamarpc.com
# VITE_BRIDGE_RPC_BSC=https://binance.llamarpc.com
# VITE_BRIDGE_RPC_POL=https://polygon.llamarpc.com
# VITE_BRIDGE_RPC_ARB=https://arbitrum.llamarpc.com
# VITE_BRIDGE_RPC_TRX=https://api.trongrid.io
